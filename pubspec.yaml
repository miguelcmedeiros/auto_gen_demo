name: auto_gen_demo
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  functional_data:

dev_dependencies:
  flutter_test:
    sdk: flutter

builders:
  functional_data_generator:

flutter:
  uses-material-design: true
